server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/accounts?useSSL=false&serverTimezone=UTC
    username: root
    password: Akki143@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # or `validate` or `none` for production
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true

#  # Optional: Use your schema explicitly
#  sql:
#    init:
#      platform: mysql

